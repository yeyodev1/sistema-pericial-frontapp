# La URL base se auto-detectiona según el hostname:
#   localhost/127.0.0.1 → http://localhost:8100
#   *.bakano.ec         → https://testing-storybrand-backapp.bakano.ec
#   cualquier otro      → VITE_API_BASE_URL (fallback)
VITE_API_BASE_URL=http://localhost:8100
